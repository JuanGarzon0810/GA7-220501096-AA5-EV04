# Copiar este archivo como ".env" y ajustar los valores segun el entorno local

PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=barbiere11

# Clave secreta usada para firmar los tokens de sesion (JWT)
JWT_SECRET=barbiere11_clave_secreta_cambiar_en_produccion
